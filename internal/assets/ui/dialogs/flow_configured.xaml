<?xml version="1.0" encoding="UTF-8"?>
<Grid xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Width="560" Height="140">

    <Grid.RowDefinitions>
        <RowDefinition Height="24"/>
        <RowDefinition Height="8"/>
        <RowDefinition Height="60"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="30"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="170"/>
        <ColumnDefinition Width="170"/>
        <ColumnDefinition Width="90"/>
    </Grid.ColumnDefinitions>

    <TextBlock x:Name="header" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4" FontWeight="Bold"
               Text="{Loc Dialog.FlowConfigured.Header}"/>
    <TextBlock x:Name="text" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="4" TextWrapping="Wrap"
               Text="{Loc Dialog.FlowConfigured.Text}"/>

    <Button x:Name="change" Grid.Row="4" Grid.Column="1" Margin="0,0,6,0"
            Content="{Loc Dialog.FlowConfigured.Change}"/>
    <Button x:Name="switchOff" Grid.Row="4" Grid.Column="2" Margin="0,0,6,0"
            Content="{Loc Dialog.FlowConfigured.SwitchOff}"/>
    <Button x:Name="cancel" Grid.Row="4" Grid.Column="3"
            Content="{Loc Dialog.FlowConfigured.Cancel}"/>
</Grid>
